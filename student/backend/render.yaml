services:
  - type: web
    name: elective-navigator-student
    env: docker
    region: ohio
    plan: free
    branch: main
    # Render maps its external port to the EXPOSE port in Dockerfile by default
